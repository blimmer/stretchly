name: macOS build

on:
  push:
    branches:
      - release
      - m1-mac-build

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v2
      with:
        node-version: '16.9.1'
    - run: npm install npm -g
    - run: npm install
    - run: node_modules/.bin/electron-builder build --publish always
      env:
        GH_TOKEN: ${{ secrets.github_token }}
